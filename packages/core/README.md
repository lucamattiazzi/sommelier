# @lucamattiazzi/sommelier-core

Headless session, policy, protocol 0.1, events, and error primitives used by Sommelier direct-agent mode.
It has no Office.js or UI dependency. Reads run automatically and mutations require approval by
default. See the [direct-agent protocol](../../docs/agent-protocol.md).
