---
"@lucamattiazzi/sommelier": patch
---

Expose the portable bridge as `sommelier-session` and use npx in the TaskPane pairing prompt,
without a global installation, temporary project, or preinstalled skill.
